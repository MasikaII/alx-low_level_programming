#include <stdio.h>
/**
 * main - Entry point to the program
 * Description: Using printf to display a message
 * Return: Always 0(Success)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
