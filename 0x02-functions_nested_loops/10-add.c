#include <stdio.h>
#include "main.h"
/**
 * add - adds two numbers
 * @x: first number
 * @y: second number
 * Return: result of addition
*/
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
